# Algonquin Fork Boundary

This repository is the Algonquin organization fork of `fpsdc-cloud`. Keep the
source compatible with `upstream`; place Algonquin environment composition,
addresses and policy in `algonquin-deployment`. Offer reusable fixes upstream.

